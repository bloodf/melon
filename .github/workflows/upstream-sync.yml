# Keep bloodf/melon:main current with deepseek-ai/deepseek-harness:master.
# main is the Melon product branch. Daily/manual job merges upstream master
# into main. Do not land Melon-only work on Melon master; that name tracks
# the upstream default only when used at all.
name: Upstream sync

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

permissions:
  contents: read
  issues: read

concurrency:
  group: upstream-sync
  cancel-in-progress: false

jobs:
  update-main:
    name: Merge upstream master into main
    runs-on: ubuntu-24.04
    permissions:
      contents: write
      issues: write
    steps:
      - name: Check out Melon main
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: main

      - name: Merge upstream master
        id: merge
        env:
          GITHUB_TOKEN: ${{ github.token }}
          UPSTREAM_REPO: deepseek-ai/deepseek-harness
        run: |
          set -euo pipefail
          git config user.name "melon-sync"
          git config user.email "melon-sync@users.noreply.github.com"
          git config core.hooksPath /dev/null
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          if git remote get-url upstream >/dev/null 2>&1; then
            git remote set-url upstream "https://github.com/${UPSTREAM_REPO}.git"
          else
            git remote add upstream "https://github.com/${UPSTREAM_REPO}.git"
          fi
          git fetch --no-tags upstream master
          git fetch --no-tags origin main
          git checkout -B main origin/main

          upstream_sha="$(git rev-parse upstream/master)"
          echo "upstream_sha=${upstream_sha}" >> "$GITHUB_OUTPUT"

          if git merge-base --is-ancestor upstream/master HEAD; then
            echo "main already contains ${upstream_sha}"
            echo "status=up_to_date" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          if git merge --no-edit --no-ff --no-verify upstream/master; then
            echo "status=merged" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          conflict_files="$(git diff --name-only --diff-filter=U || true)"
          {
            echo "status=conflict"
            echo "conflict_files<<EOF"
            echo "${conflict_files}"
            echo "EOF"
          } >> "$GITHUB_OUTPUT"
          git merge --abort || true
          echo "Merge conflict; main left at origin/main."

      - name: Push main
        if: steps.merge.outputs.status == 'merged'
        run: git push origin main

      - name: File conflict issue
        if: steps.merge.outputs.status == 'conflict'
        env:
          GITHUB_TOKEN: ${{ github.token }}
          UPSTREAM_SHA: ${{ steps.merge.outputs.upstream_sha }}
          CONFLICT_FILES: ${{ steps.merge.outputs.conflict_files }}
        run: |
          set -euo pipefail
          existing="$(gh issue list --label upstream-sync-conflict --state open --json number --jq '.[0].number' || true)"
          files_body="$(printf '%s' "${CONFLICT_FILES}" | sed '/^$/d')"
          header="Upstream sync conflict on \`${UPSTREAM_SHA}\`"
          body="Automated merge of \`deepseek-ai/deepseek-harness:master\` (${UPSTREAM_SHA}) into Melon \`main\` failed."$'\n\n'"## Conflicting files"$'\n\n'"\`\`\`"$'\n'"${files_body}"$'\n'"\`\`\`"$'\n\n'"Melon \`main\` was not pushed. Resolve on a worktree, keep Melon seams, then re-run."
          if [ -n "${existing}" ]; then
            gh issue edit "${existing}" --title "${header}" --body "${body}"
            gh issue comment "${existing}" --body "Re-run still failing on \`${UPSTREAM_SHA}\`."
          else
            gh issue create --title "${header}" --label "upstream-sync-conflict" --body "${body}"
          fi
