{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "setup",
  "description": "Local bundled setup UI commands and progress events.",
  "windows": ["setup"],
  "permissions": [
    "core:event:default",
    "allow-status",
    "allow-probe",
    "allow-activate",
    "allow-shutdown"
  ]
}
